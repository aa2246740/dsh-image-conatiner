//#region lib/types/index.js
const name = "dsh-image-conatiner";
const inject = [];
/** Mount the package so its browser declaration joins the Web plugin graph. */
function apply(_ctx) {
	console.log("[my-plugins/dsh-image-conatiner] loaded");
}
//#endregion
export { apply, inject, name };
