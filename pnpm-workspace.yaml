allowBuilds:
  esbuild: true

# Official 0.1.5-rc.3 packages publish the same day as the desk target.
# A 24h release-age gate would refuse that install. 0.1.7 alphas stay out
# because devDependencies pin 0.1.5-rc.3 exactly.
minimumReleaseAgeExclude:
  - '@deepseek-ai/*'
